BEGIN

FOR loan_rec IN
(
    SELECT c.Name,
           l.LoanID,
           l.ENDDate

      FROM Customers c
      
      JOIN Loans l
      
      ON c.CustomerID=l.CustomerID

      WHERE l.ENDDate
      BETWEEN SYSDATE
      AND SYSDATE+30
      )
      LOOP
      
      DBMS_OUTPUT.PUT_LINE(
      'Reminder : '||loan_rec.Name||' Loan Due on '||loan_rec.ENDDate);

END LOOP;
END;
/
